services:
  litespeed-agent:
    build:
      context: ../..
      dockerfile: dev/litespeed/Dockerfile
    hostname: "litespeed-box"
    working_dir: /app
    volumes:
      - ../../:/app
    ports:
      - "8445:8443"
      - "8446:7080"
    env_file:
      - ./.env
